#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

// 该文件为原始文件 具体实现在各个文件中

#define ERR_EXIF(m) \
        do \
        { \
            perror(m);  \
            exit(EXIT_FAILURE); \
        } while(0);

int main() {
    return 0;
}